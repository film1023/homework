#include <stdio.h>
#include <stdlib.h> 
#include <string.h> 
#include <ctype.h>  

int is_positive_integer(const char *str, int *number) {
    if (str == NULL || *str == '\0') {
        return 0; 
    }
    
    for (size_t i = 0; str[i] != '\0'; i++) {
        if (!isdigit(str[i])) {
            return 0; 
        }
    }
    *number = atoi(str);
    if (*number < 0 || *number > 100) {
        
    }
    
    return 1; 
}


int main() {
    char input[100]; 
    int score;
    char grade[5]; 
    
    printf("enter score: ");
    if (fgets(input, sizeof(input), stdin) == NULL) {
        return 1; 
    }
    input[strcspn(input, "\n")] = 0;
    if (!is_positive_integer(input, &score)) {
        printf("please enter number only.\n");
        return 0;
    }
    if (score < 50) {
        strcpy(grade, "F");
    } 
    else if (score >= 50 && score <= 55) {
        strcpy(grade, "D");
    } 
    if (score < 50) {
        strcpy(grade, "F");
    } 
    else if (score >= 50 && score <= 54) {
        strcpy(grade, "D");
    }
    else if (score >= 55 && score <= 60) {
        strcpy(grade, "D+");
    } 
    else if (score >= 80) {
        strcpy(grade, "A");
    }
    else if (score >= 75) { 
        strcpy(grade, "B+");
    }
    else if (score >= 70) {
        strcpy(grade, "B");
    }
    else if (score >= 65) {
        strcpy(grade, "C+");
    }
    else if (score >= 60) {
        strcpy(grade, "C");
    }
    else if (score >= 55) {
        strcpy(grade, "D+");
    }
    else if (score >= 50) {
        strcpy(grade, "D");
    }
    else {
        strcpy(grade, "F"); 
    }
    printf("%s!\n", grade);    
    return 0;
}
