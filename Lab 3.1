#include <stdio.h>

int main() {
    float H;
    float W;
    float Area;

    printf("Input H (Height): ");
    scanf("%f", &H);
    printf("Input W (Base): ");
    scanf("%f", &W);

    Area = (H * W) / 2.0; 

    printf("\nOutput:\n");
    printf("Area = %.1f\n", Area); 
    return 0;
}
